<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <androidx.media3.ui.PlayerView
        android:id="@+id/playerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:use_controller="false"
        app:resize_mode="fit" />

    <!-- Entrada numérica directa de canal (0-9), esquina superior derecha -->
    <TextView
        android:id="@+id/numberEntryOverlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_margin="24dp"
        android:background="#CC000000"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:visibility="gone" />

    <!-- OSD: banner inferior con número, logo, nombre y candado -->
    <LinearLayout
        android:id="@+id/osdRoot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#CC0F1420"
        android:padding="16dp"
        android:visibility="gone"
        android:alpha="0">

        <TextView
            android:id="@+id/osdNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#8B93F0"
            android:textSize="22sp"
            android:fontFamily="monospace" />

        <ImageView
            android:id="@+id/osdLogo"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="16dp" />

        <TextView
            android:id="@+id/osdName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:maxLines="1" />

        <ImageView
            android:id="@+id/osdLock"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:src="@drawable/ic_lock"
            android:visibility="gone" />
    </LinearLayout>
</FrameLayout>
